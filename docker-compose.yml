version: "3.9" 
services:
  bot:
    container_name: VoenmehWorkFrilance.bot
    build:
      context: ./
      dockerfile: ./bot/Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - custom

networks:
  custom:
    driver: bridge